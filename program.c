#include <stdio.h>
#include <stdlib.h>

int main() {
		scanf("%d", n);
		int prom  =  0;
		int i  =  0;
	while(i < n){
		scanf("%d", cal);
		int prom  = prom + cal;

	}
		int prom  = prom / n;
	if(prom > 5){
		printf("Aprobado con: ", prom);
	}
	if(prom == 10){
		printf("Excelente");
	}
	return 0;
}